<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.octogone">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/mon_icone"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ChoixSurviActivity">
        </activity>

        <activity android:name=".Survi.SurviActivity">
        </activity>

        <activity android:name=".OptionsActivity">
        </activity>

        <activity android:name=".Survi.GOActivity">
        </activity>

        <activity android:name=".MenuBossActivity">
        </activity>

        <activity android:name=".Combat_boss.BossActivity">
        </activity>
        <activity android:name=".Combat_boss.GOBossActivity">
        </activity>
    </application>

</manifest>